#include "SkillProcessor.h"
#include "Entity.h"
using namespace JSB;

void SkillProcessor::process(const SkillResult& sr, Environment& env)
{
	//for (auto& i : sr.casts)
	//{
	//	switch (i.type)
	//	{
	//	case CT_ATTACK:
	//		Entities&& ents = env.getTarget(i.target);
	//		for (auto& e : ents)
	//		{
	//			process(e->hurt(i), env);
	//		}
	//		break;
	//	case CT_HEAL:
	//	}
	//}
}
